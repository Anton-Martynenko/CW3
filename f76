function amountOfPages(summary){
  if(summary <= 9) return summary
  let count = 0;
  let result = 0;
  for(let i = 1; i < summary; i++){
    count+=i.toString().length
    if (count == summary) result = i
  }
  return result;
}
